<form method="post" action="{{ path('gutschein_delete', {'id': gutschein.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gutschein.id) }}">
    <button class="btn">Löschen</button>
</form>
